- make exit with removing of all elements (including overlay)
    - via Esc
    - via close button
- make background for the numbers
- ignore right click
- move / resize using keyboard (arrows / shift + arrows)
- show numbers outside of the ruler if the ruler is too small
- show left, top (and right, bottom?)

- add favicon
- source map (doesn't work now)

- cannot resize if size became too small